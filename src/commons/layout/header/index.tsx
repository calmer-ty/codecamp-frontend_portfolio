import styled from "@emotion/styled";

const Wrapper = styled.div`
  height: 160px;
  background-color: yellow;
`;

export default function LayoutHeader(): JSX.Element {
  return (
    <>
      <Wrapper>Header === Header === Header === Header</Wrapper>
    </>
  );
}
