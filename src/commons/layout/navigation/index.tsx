import styled from "@emotion/styled";

const Wrapper = styled.div`
  height: 64px;
  background-color: #ffd600;
`;

export default function LayoutNavigation(): JSX.Element {
  return (
    <>
      <Wrapper>Navigation === Navigation === Navigation === Navigation</Wrapper>
    </>
  );
}
