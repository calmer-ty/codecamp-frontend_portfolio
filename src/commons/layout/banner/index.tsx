import styled from "@emotion/styled";

const Wrapper = styled.div`
  height: 400px;
  background-color: blue;
`;

export default function LayoutBanner(): JSX.Element {
  return (
    <>
      <Wrapper>Banner === Banner === Banner === Banner</Wrapper>
    </>
  );
}
